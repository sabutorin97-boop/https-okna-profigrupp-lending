<?php
/**
 * Образец настроек приёма заявок.
 *
 * На сервере скопируйте этот файл рядом, под именем config.php,
 * и впишите настоящие данные:
 *
 *     cp config.sample.php config.php
 *     nano config.php
 *
 * config.php намеренно не хранится в репозитории — в нём пароли и токены.
 * Если файла нет, сайт всё равно работает: заявки записываются в журнал,
 * просто письма и сообщения не отправляются.
 */

return [
    // Куда приходят заявки на почту
    'recipient' => 'potolok-45@yandex.ru',

    // Где хранить журнал заявок. Каталог должен быть ВНЕ папки сайта,
    // иначе заявки можно будет скачать из браузера.
    'leads_dir' => '/var/www/okna-leads',

    /**
     * Метка источника — с какого сайта пришла заявка.
     * Подставляется в тему письма и в сообщение в мессенджере.
     * Обычно её присылает сама форма, здесь — запасное значение.
     */
    'source_label' => 'Квиз форма',

    /**
     * Отправка письма через SMTP.
     *
     * Для Яндекса нужен пароль приложения, а не обычный пароль от почты:
     * id.yandex.ru → Безопасность → Пароли приложений → Почта.
     *
     * Поля 'user' и 'from' должны совпадать — Яндекс не даёт отправлять
     * письма от чужого имени.
     */
    'smtp' => [
        'enabled'   => false,
        'host'      => 'smtp.yandex.ru',
        'port'      => 465,
        'user'      => 'potolok-45@yandex.ru',
        'password'  => 'сюда-пароль-приложения',
        'from'      => 'potolok-45@yandex.ru',
        'from_name' => 'Сайт «Окна Профигрупп»',
    ],

    /**
     * Дублирование заявок в мессенджер MAX.
     *
     * Как получить token и chat_id:
     *   1. зайдите в кабинет business.max.ru, раздел «Чат-боты» → «Создать»
     *      (командами в переписке бота не завести, только через кабинет);
     *   2. заполните форму — название, телефон, сайт, описание, логотип
     *      500×500 — и заберите выданный токен;
     *   3. напишите своему боту любое сообщение, например «привет»;
     *   4. на сервере выполните:
     *          php /var/www/okna/max-chat-id.php ВАШ_ТОКЕН
     *      скрипт покажет chat_id — впишите его сюда.
     *
     * Поля base_url, auth и recipient_field менять нужно только если
     * MAX изменит формат обращения к API. Значения по умолчанию —
     * самые распространённые.
     */
    'max' => [
        'enabled'         => false,
        'token'           => 'сюда-токен-бота',
        // Лучше указать одну группу, где сидят все, кому нужны заявки.
        // Если группа не получилась — можно перечислить чаты через запятую:
        // 'chat_id' => '304183059, 323474982', тогда заявка уйдёт каждому.
        'chat_id'         => 'сюда-chat_id',
        'base_url'        => 'https://platform-api.max.ru',
        // 'header' — голый токен, 'bearer' — с приставкой Bearer,
        // 'query' — параметром в адресе. Нужный подскажет max-chat-id.php.
        'auth'            => 'header',
        'recipient_field' => 'chat_id',  // 'chat_id' или 'user_id'
    ],

    /**
     * Заявка ещё и в CRM (crm-okna) — отдельным запросом, помимо журнала,
     * письма и MAX.
     *
     * url — адрес приёмника заявок в CRM, вида
     *       https://<домен-crm>/api/leads-intake
     * secret — тот же секрет, что записан в CRM как переменная окружения
     *          LEADS_INTAKE_SECRET (Lovable Cloud → Secrets). Без совпадения
     *          CRM отвечает 401 и заявка в неё просто не попадёт — на письмо
     *          и MAX это не влияет.
     * source_key — как заявка подпишется в CRM, справочник каналов там же:
     *              для этого сайта — 'landing_windows'.
     */
    'crm' => [
        'enabled'    => false,
        'url'        => 'https://сюда-домен-crm/api/leads-intake',
        'secret'     => 'сюда-секрет-из-crm',
        'source_key' => 'landing_windows',
    ],
];
